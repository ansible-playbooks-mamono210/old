- name: テストスクリプトを配置するディレクトリを作成する
  file:
    path: '{{ ansible_user_dir }}/ansible/bin/tests'
    recurse: yes
    state: directory

- name: shunit2をGitリポジトリから取得する
  git:
    repo: https://github.com/kward/shunit2.git
    dest: '{{ ansible_user_dir }}/ansible/bin/shunit2'
